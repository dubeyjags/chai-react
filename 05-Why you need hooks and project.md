npm create vite@latest

const [var, setVar] = useState(initVale);

var is the default value of state
setVar is the function to update the state var
initVal is the default value of the var

value update at once at everyplace by setVar function

