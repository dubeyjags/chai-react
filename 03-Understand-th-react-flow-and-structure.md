html page need to call the js with script tags

folder structure
jsx is the main power of the react
main App is the function which return functions
programing capbailty in the html
app function can call any other same type of funtions
need to export and include at the App.jsx (APP)
component name need to Uppercase
and it need single element returns like fragements
can be add mupltiple eleements
it called components